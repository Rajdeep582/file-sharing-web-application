spring.application.name=fileSharingApp
server.port=9090

# ? MySQL connection (replace with your actual DB host if different)
spring.datasource.url=jdbc:mysql://mysql.render.com:3306/cw_files_prj
spring.datasource.username=root
spring.datasource.password=root@029

# ? Hibernate dialect (updated)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ? File upload limits
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ? Logging
logging.level.org.springframework.web=DEBUG

# ? Google OAuth (redirect URI updated for Render)
spring.security.oauth2.client.registration.google.client-id=YOUR_GOOGLE_CLIENT_ID
spring.security.oauth2.client.registration.google.client-secret=YOUR_GOOGLE_CLIENT_SECRET
spring.security.oauth2.client.registration.google.redirect-uri=https://bytebox.onrender.com/login/oauth2/code/google

# ? GitHub OAuth
spring.security.oauth2.client.registration.github.client-id=YOUR_GITHUB_CLIENT_ID
spring.security.oauth2.client.registration.github.client-secret=YOUR_GITHUB_CLIENT_SECRET